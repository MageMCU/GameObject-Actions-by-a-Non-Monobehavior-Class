# Source Examples

This folder contains a minimal split between a MonoBehaviour and a non-MonoBehaviour class.

- PlayerController.cs: Unity-facing script (lifecycle, input, scene references).
- Mover.cs: Plain C# logic class (movement rules).

Use this split when you want cleaner game logic that is easier to test and maintain.
